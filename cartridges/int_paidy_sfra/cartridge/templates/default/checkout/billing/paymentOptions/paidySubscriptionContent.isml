<div class="tab-pane paidy-subscription-content" id="paidy-subscription-content" role="tabpanel">
    <fieldset class="payment-form-fields">
        <!--- payment method is paidy subscription --->
        <input type="hidden" class="form-control"
               name="${pdict.forms.billingForm.paymentMethod.htmlName}"
               value="${Resource.msg('paidy.payment.method.subscription.type', 'paidy', null)}"
        >
        <iscomment>Unless encoding is "off", HTML described in the property file cannot be used.</iscomment>
        <isprint value="${Resource.msg('paidy.payment.content', 'paidy', null)}" encoding="xmlcomment" >
    </fieldset>
</div>
